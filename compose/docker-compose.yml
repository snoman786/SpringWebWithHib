services:
  testdb:
    image: snoman1/spring-hib-db:V1
    ports:
      - "3306:3306"
    volumes:
      - testdbdata:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
  testapp:
    image: snoman1/spring-hib-app:V1
    ports:
      - "7070:8080"
volumes:
  testdbdata: {}